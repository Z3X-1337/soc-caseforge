# Contributing

- Use sanitized evidence only.
- Add tests for every parser, rule, schema, or report change.
- Keep findings deterministic and explainable.
- Document false-positive conditions and analysis limitations.
- Run `python -m unittest discover -s tests -v` before submitting changes.
